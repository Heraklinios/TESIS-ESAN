using UnityEngine;
using System.Collections;
using thelab.mvc;
public class TesisApplication : BaseApplication<TesisModel,TesisView,TesisController> {

	// Use this for initialization
	void Start () {
	
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
